pipeline {
    agent any
    
    stages {
        stage('terraform infa create') {
            steps {
                sh """cd solution_files
                terraform init
                terraform apply --auto-approve
                """
            }
        }
       
    }
}
