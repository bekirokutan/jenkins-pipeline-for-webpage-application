pipeline {
    agent any
    
    stages {
        stage('terraform infa create') {
            steps {
                sh """
                cd solution_files
                pwd
                ls
                terraform init
                terraform apply --auto-approve
                """
            }
        }
       
    }
}
